# Tuple -> Immutable Lists (Can't change or reorder the values)
my_info = ("Roxy", 20, "Student")

name, age, occupation = my_info # Unpacking the tuple
print(name)

one_element_tuple = (4,) # Comma is necessary to indicate that it's a tuple (trailing comma)
